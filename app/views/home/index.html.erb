<h1>Flats<h1>
<h3>Boas vindas ao sistema de locação de apartamentos</h3>
<nav>
<%= link_to 'Adicionar propriedade',  new_property_path%>
<%= link_to 'Adicionar tipo de propriedade',  new_property_type_path%>

</nav>
<% @properties.each do |p| %>
    <%=link_to p.title, property_path(p.id)%>
    <%= p.description%>
  Quartos: <%= p.rooms%>
<% end %>
  
